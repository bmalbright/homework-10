const Intern = require("./lib/intern.js")

describe("Intern", () => {
    describe("initialization", () => {
      // 'reverse' should reverse "Hello World!"
      it("creates the Intern subclass under the Employee class", () => {
          const Intern = new IntersectionObserverEntry("Zeke", 26, "zeke@zeke.com", 201)

        expect(Intern.name).toEqual("string");
        expect(Intern.id).toEqual(num);
        expect(Intern.email).toEqual(email);
        expect(Intern.college).toEqual("string");
      });


    });